[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::sqlalchemy.exc.LegacyAPIWarning
markers =
    smoke: jornadas críticas do aluno real do IBC (Fase 5 do playbook 0.9 — scripts/rehearsal.sh roda só este subconjunto contra staging)
