# DÉBITOS — Comportamentos legados registrados na caracterização (Fase 1)

Regra do playbook: testes de caracterização **documentam** o que o sistema faz
hoje; nada aqui foi "corrigido" durante a Fase 1. Cada item vira decisão
consciente (corrigir, conviver ou aposentar) — a maioria se resolve
naturalmente na reestruturação da Release 0.9/1.0.

| # | Débito | Evidência | Observação |
|---|---|---|---|
| 1 | **Três sistemas de nível concorrentes:** `calculate_level()` é linear hardcoded (100 pts/nível, teto 7) e ignora tanto a tabela `Level` (editável pelo admin em `/api/admin/levels` — edição sem efeito real) quanto os `points_*` do `PlatformConfig` (config de pontos do admin ignorada pelos `POINTS_PER_ACTION` hardcoded) | `tests/test_characterization_gamification.py::test_nivel_sobe_a_cada_100_pontos` · `routes/gamification.py:14-43` | Já apontado no ROADMAP.md #11; alinhar com GAM-01 na 0.9/1.0 |
| 2 | `points_in_level` cresce sem teto no nível 7 (2000 pts → "1400 no nível") | mesmo teste acima | Cosmético; UI mostra número estranho |
| 3 | **Dois sistemas de conquistas concorrentes:** `Badge/UserBadge` (legado) e `Achievement/UserAchievement` (Sprint 6.1) coexistem e são exibidos em lugares diferentes | `routes/gamification.py` (comentário na própria rota) | Consolidar em um só na 0.9/1.0 |
| 4 | **Progresso duplicado:** tabela `Progress` (por curso, legado) e `LessonProgress` (por aula) registram avanço em paralelo; dashboards misturam as fontes | `routes/progress.py` vs `routes/lessons.py` | PRD CUR-01 consolida em `progresso_licoes` |
| 5 | Quiz legado de curso (`POST /api/progress/quiz/<id>/submit`) **revela o gabarito completo mesmo errando** — diferente do quiz de aula, que oculta ao reprovar | `tests/test_characterization_progress_questions.py::test_quiz_legado_do_curso_corrigido_no_servidor` | Fluxo aparentemente não usado pelo SPA atual (que usa o quiz de aula); candidato a aposentadoria |
| 6 | Badge `servo_fiel` (streak 7 dias) é **inalcançável**: `_consecutive_days()` retorna sempre 0 | `routes/gamification.py:128-129` | Streaks server-side são GAM-02 (Release 1.0); badge exibido como 0/7 para sempre |
| 7 | Badge `corredor_incansavel` idem: progresso fixo `0/1` | `routes/gamification.py:106-107` | |
| ~~8~~ | ~~Catálogo (`GET /api/courses`) é público e lista cursos `acesso='interno'` para anônimos (o detalhe bloqueia, a listagem não)~~ **RESOLVIDO** (revisão de segurança, jul/2026): listagem agora filtra `acesso != 'interno'` pra quem não está autenticado, mesmo corte que o detalhe já aplicava | `routes/courses.py::list_courses` · `tests/test_characterization_courses.py::test_anonimo_nao_lista_curso_interno` | — |
| 9 | Modelos usam emoji como default (`Course.icon='📖'`, `Trail.icon='🛤️'`) — contradiz a diretriz "zero emoji nativo" do design system | `models.py` | UX_ALUNO.md B2; trocar por código de ícone vetorial |
| 10 | Flask-Limiter com storage em memória (warning em todo boot) — funciona com `--workers 1`, quebra silenciosamente com réplicas (doc 02 prevê 2×) | warning no boot | Redis entra na Fase 4 do playbook; migrar o limiter junto |
| 11 | `/health` responde `db: connected` sem consultar o banco | `app.py` | Enganoso para monitoração |
| 12 | N+1 em serialização: `Course.to_dict` (category_rel), `Question.to_dict` (author), `TrailCourse.to_dict` (course) — listagens disparam 1 query por linha | revisão de código (jul/2026) | Irrelevante no volume atual; tratar nos repositórios da 0.9 |
| 13 | Schema nasce de `db.create_all()` — sem Alembic, sem migração versionada | `app.py:106` | Resolvido na Fase 2 do playbook (Alembic entra com tenancy) |
| 14 | `User.onboarding_completed` é flag GLOBAL: concluir o onboarding num tenant marca concluído em todos (a resposta `OnboardingAnswer` em si é por tenant desde o grupo 2) | `routes/trails.py::onboarding_status` | Resolver na Fase 4 junto com papéis por tenant (`tenant_users`) |
| 15 | `forgot-password` ainda tem um resíduo de timing side-channel: e-mail existente dispara envio SMTP **síncrono** antes de responder (e-mail inexistente responde na hora) — a diferença de status/corpo já foi corrigida (revisão de segurança, jul/2026), mas o tempo de resposta ainda varia | `routes/auth.py::forgot_password` | Mitigação completa exige enviar o e-mail num worker assíncrono (RQ — Release 1.0); fora do escopo da correção de enumeração |
| 16 | `POST /api/admin/users/<id>/toggle-active` e `POST /api/admin/users/<id>/reset-password` foram desativados para admin de tenant (correção HIGH-1, revisão de segurança jul/2026): `is_active`/senha são atributos GLOBAIS do `User`, e não existe hoje uma coluna "ativo NO tenant" em `tenant_users` para dar ao admin um controle equivalente sem afetar outros tenants | `routes/admin.py::toggle_user_active`, `::admin_reset_user_password` | Requer migração (`tenant_users.active_in_tenant` ou papel `operador_plataforma`) — Release 1.0; até lá, revogar acesso a um tenant é feito removendo o vínculo (`DELETE /api/auth/users/<id>`) |
| 17 | `award_points` faz leitura-modificação-escrita não protegida em `total_points` para ações no MESMO dia (após o guarded UPDATE de streak decidir o vencedor) — duas ações concorrentes do mesmo usuário no mesmo dia podem perder pontos (lost update clássico) | `routes/gamification.py::award_points` (2ª revisão Fable 5, GAM-04, jul/2026) | Mesma classe de risco já aceita em outros lugares do código; baixo impacto prático (poucos pontos, raro na prática) — considerar o mesmo padrão de UPDATE guardado usado pro streak se virar problema real |
| 18 | `award_points` pode reportar `level_up`/`new_level` espúrio pro request que perde a corrida do streak (guarded UPDATE): `old_level` é lido antes do possível `db.session.refresh(up)` | `routes/gamification.py::award_points` (2ª revisão Fable 5, GAM-04, jul/2026) | Cosmético — o valor final salvo no banco está sempre correto, só a resposta HTTP de uma requisição perdedora pode exagerar o level_up momentaneamente |
| 19 | Docstring de `streak_efetivo()` ainda diz "só é zerado de verdade no próximo LOGIN" — desde a correção do streak (GAM-04, jul/2026) qualquer ação em `_ACOES_QUE_CONTAM_STREAK` reinterpreta/reinicia, não só o login | `routes/gamification.py::streak_efetivo` | Drift de documentação, sem efeito em comportamento; atualizar comentário |
| 20 | `aria-live="polite"` dos contadores do dashboard (streak/pontos) é recriado do zero a cada re-render completo do grupo (innerHTML do pai é substituído) — tecnicamente presente, mas pode não disparar anúncio em todo leitor de tela porque o nó não é "atualizado", é recriado | `index.html` (região `dash-saas-pontuacao`, 2ª revisão Fable 5, GAM-04, jul/2026) | Atende a letra do gate de acessibilidade (§5); pra atender a intenção completa, precisaria atualizar o `textContent` de um nó persistente em vez de substituir o innerHTML do container inteiro |
| 21 | **Performance mobile do dashboard SaaS abaixo do gate do UX_ALUNO_SAAS.md §5** (Lighthouse real, mobile, 375×667/DPR2, medido em jul/2026): login **64/100**, dashboard autenticado **62/100** — gate pede ≥85. Causa provável, pelo relatório do Lighthouse (Speed Index 21.2s, score 0; `network-dependency-tree-insight` zerado): ~35 `<link rel="stylesheet">` individuais sem bundle/minificação no `<head>`, XHR síncrono de tema bloqueando o parse (necessário pra evitar flash de tema errado — ver Etapa 1), e scripts de CDN externos (pdf.js, Chart.js, SortableJS) sem `async`/`defer`. Acessibilidade passa no dashboard (96/100, ≥95) mas fica abaixo no login (89/100) — dois achados reais: `color-contrast` e `label-content-name-mismatch` | relatórios JSON completos gerados via `npx lighthouse` real contra servidor local (não estimativa) | **Decisão consciente de priorização, não item esquecido**: registrado e aceito explicitamente antes do merge pra `claude/zen-hopper-SeRac` — os 6 bloqueadores de segurança/correção/acessibilidade funcional foram tratados primeiro; otimização de performance (bundling/minificação de CSS, defer de scripts de CDN, revisão de contraste) fica para a Release 1.0 ou um PR dedicado, não bloqueia esta entrega |
| 22 | **`.dash-saas-pontuacao-total` não renderiza no tamanho de fonte pretendido (48-56px, P0.2 de docs/MELHORIAS-UI-ALUNO.md)** — computa 16px mesmo com a única regra CSS que casa o elemento (confirmado via Chrome DevTools Protocol `CSS.getMatchedStylesForNode`, sem nenhuma regra concorrente) declarando `font-size: var(--text-display-lg)`/`var(--text-display-xl)`, testado com `!important`, com valor literal em pixels (sem `var()` nenhum) e até via atributo `style=""` inline no próprio HTML — todas as formas falharam da mesma maneira nesta stack de teste (chromium headless, `/opt/pw-browsers/chromium`). Um HTML mínimo isolado com a MESMA regra CSS funciona normalmente (48px). `font-weight` na mesma declaração aplica normalmente; só `font-size` falha. Observação lateral não confirmada como causa: `GET /api/aluno/dashboard` aparece duplicado nos logs de request durante o carregamento do dashboard (possível dupla chamada de `_dashSaasCarregarTudo`), não investigada até o fim por tempo | `tests/e2e/test_dashboard_saas.py::test_card_de_pontuacao_usa_gradiente_da_marca_e_contem_a_chama_de_streak` (asserção de font-size marcada `pytest.xfail` explicitamente, as outras duas do mesmo teste — gradiente de fundo e chama de streak dentro do card — continuam validando normalmente) | **Não resolvido nesta rodada** (GAM-05 PR 2) apesar de investigação extensa (renomear a custom property, desabilitar cache via CDP, contexto de browser limpo, comparar com outros var()-based font-size da mesma página que funcionam, testar HTML isolado). Recomendação: investigar a duplicação de `GET /api/aluno/dashboard` primeiro (pode ser causa raiz ou pista) antes de tentar mais workarounds de CSS; se persistir, considerar medir com um browser real (não headless) ou outro build de Chromium pra isolar se é peculiaridade deste ambiente de sandbox |
| 23 | **Contraste do texto sobre `--brand-gradient` falha AA (4.5:1) pra cor padrão do IBC** — `core/theming.py::cor_sobre_gradiente` (GAM-05 PR 2) já escolhe a MELHOR opção possível entre branco/escuro via maximin (pior-caso contra as duas pontas do gradiente), mas pro teal padrão do IBC (`#008ea8` → `#0056a8` após rotação de matiz +20°) nenhuma das duas opções alcança 4.5:1 nas duas pontas ao mesmo tempo: branco dá 3.86:1 contra a ponta teal (falha) e 7.25:1 contra a ponta azul (passa); a plataforma está usando o próprio tema padrão sem atingir o gate de acessibilidade que ela mesma define pros tenants (§2.2.4) | `core/theming.py::cor_sobre_gradiente`; medido via `razao_de_contraste` reaproveitada do próprio módulo | Corrigir exige mexer no ALGORITMO do gradiente (não só na escolha de texto) — ex.: limitar a rotação de matiz ou a diferença de luminosidade entre as duas pontas o suficiente pra garantir que QUALQUER cor de texto escolhida bata 4.5:1 nas duas pontas — escopo maior que esta PR, fica para P2/Release 1.0 |
| 24 | **Fallback de tenant padrão em `TenantScopedModel.tenant_id` pode mascarar um bug real no billing** — `tenant_id` de qualquer model tenant-scoped, se omitido em Python, cai silenciosamente no tenant padrão (`current_tenant_id()` → `default_tenant_id()`, comportamento legado de compatibilidade mono-tenant, pré-existente, não introduzido pelo módulo de billing) em vez de falhar. Achado da revisão Fable 5 do BIL-01 PR 1: um handler de webhook do Asaas (PR 2) roda FORA da resolução de tenant por subdomínio — se ele processar um evento sem resolver explicitamente o tenant certo (ex.: por `asaas_customer_id`), uma escrita em `Subscription`/`AiUsage`/`AuditLog` pode ir pro tenant padrão ('ibc') em silêncio, em vez de falhar alto. `publish_event`/`registrar_auditoria` já exigem `tenant_id` explícito como parâmetro, o que mitiga — mas não elimina o risco se o CALLER passar um `tenant_id` errado/None sem querer | `core/tenancy/models.py::TenantScopedModel` (comportamento geral, não específico do billing); risco concreto é em código futuro de `core/billing/asaas.py` (webhook handler, PR 2) | PR 2 deve resolver o tenant explicitamente a partir do `asaas_customer_id`/`asaas_subscription_id` do payload do webhook (nunca depender do fallback implícito) e falhar alto (400/log de erro) se não conseguir resolver — não silenciosamente usar o tenant padrão. Vale um teste específico provando que um webhook sem tenant resolvível NÃO escreve dado nenhum |
| 25 | **Chamadas HTTP de billing ao Asaas (`core/billing/asaas.py`) são síncronas, não backgrounded via RQ** — o playbook (PRD §4.8/doc 02 §8) prevê um serviço worker RQ no Railway, mas este repo ainda não tem NENHUMA infraestrutura de fila/worker (`workers/` não existe, nenhum RQ configurado — confirmado na pesquisa da PR 1 e revalidado na PR 2). Construir a fila inteira (worker, conexão Redis, retry de job, monitoramento) é escopo maior que esta PR (cliente HTTP + webhook + idempotência). Decisão consciente: as 3 chamadas (`criar_customer`, `criar_subscription`, `cancelar_subscription`) rodam de forma síncrona na request, mas isoladas em `core/billing/asaas.py` — nenhum outro módulo chama a API do Asaas diretamente — para que uma PR futura troque a chamada direta por um `enqueue(...)` sem tocar em quem chama | `core/billing/asaas.py` (PR 2, BIL-01/02) | Planejado para quando `workers/`/RQ forem introduzidos no repo (doc 02 §8, infraestrutura MVP prevê serviço worker separado); até lá, o tempo de resposta de rotas que criam customer/subscription no Asaas inclui a latência de rede da API externa (mitigado por timeout de 10s + retry com backoff, não por assincronia) |
| 26 | **Resolução de tenant no webhook do Asaas usa uma conexão raw (`db.engine.connect()`) pra contornar RLS por design, não como workaround temporário** — `core/billing/routes.py::_resolver_tenant_id` precisa localizar QUAL tenant é dono de um `asaas_customer_id`/`asaas_subscription_id` ANTES de saber o tenant (é assim que ele descobre) — mas `subscriptions` tem RLS (correção HIGH da revisão da PR 1), e a sessão ORM só fixa o GUC `app.tenant_id` a partir de `g.tenant`, que ainda não existe nesse ponto do request. Sem a conexão raw, a consulta cairia no modo mono-tenant (tenant PADRÃO) do listener de `core/tenancy/rls.py`, e o webhook nunca resolveria tenants que não fossem o padrão — achado CRITICAL da 2ª revisão Fable 5 da PR 2, corrigido antes do commit. A conexão raw funciona hoje porque a app ainda conecta com role COM BYPASSRLS (RLS inócuo até a troca de role no Railway, ver `core/tenancy/rls.py` e `docs/RUNBOOK-RLS.md`) — quando essa troca acontecer, esta consulta especificamente vai continuar precisando enxergar `subscriptions` de qualquer tenant, então vai precisar rodar com a role operador/admin (BYPASSRLS), o mesmo padrão que doc 02-ARQUITETURA.md §5.3 já reserva pro "painel do operador" | `core/billing/routes.py::_resolver_tenant_id` | Quando a troca de role RLS acontecer (runbook), configurar explicitamente essa consulta (e qualquer outra do mesmo tipo — resolução cross-tenant por chave externa, ex.: login por e-mail) pra usar uma conexão/role com BYPASSRLS, documentado e auditado, não a role padrão da app |
| 27 | **Duas fontes de verdade pro "plano" de um tenant** — `Tenant.plano` (TEN-01, default `'semente'`, nunca escrito depois da criação neste repo) e `Subscription.plano` (BIL-01, atualizado pelo fluxo de assinatura/Asaas) podem divergir sem aviso: um tenant que faz upgrade via checkout muda `Subscription.plano`, mas `Tenant.plano` fica parado no valor de criação — nada sincroniza as duas colunas. `core/billing/metering.py::_plano_do_tenant` (PR 4, BIL-03) usa `Subscription.plano` como autoritativo pra cota de IA (dinheiro real), caindo pra `Tenant.plano` só quando não há Subscription — mas qualquer outro código que já leia `Tenant.plano` diretamente (ex.: `core/tenancy/middleware.py`, exposto em `Tenant.to_dict()`) pode mostrar um plano desatualizado pro resto da plataforma | `core/billing/metering.py::_plano_do_tenant`; `core/tenancy/models.py::Tenant.plano`; `core/billing/models.py::Subscription.plano` | Resolver exige decidir UMA fonte de verdade e sincronizar a outra (trigger/evento de domínio na mudança de plano) — fora do escopo desta PR (stub de medição). Até lá, qualquer leitura de "qual o plano do tenant" fora de `core/billing/` deveria preferir `Subscription.plano` quando existir |
